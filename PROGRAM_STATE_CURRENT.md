# PROGRAM_STATE_CURRENT.md — canonical state snapshot

**As of:** Sat 2026-09-05 ~03:30 Europe/Zurich (≈5.5 h to deadline 08:59; new-numbers cutoff 06:00; 06:00 go/extend decision; submit by 08:30). Numbers: `docs/RESULTS_DIGEST.md` at the `replication` merge (`c852658`) or later (sole citable source). Pod terminated 14:32 Fri; adapters and caches destroyed (C seed 1's adapter survives untracked on the Mac). **The C-seed-1 replication landed (00:35 Sat, $2.48) and C_masked landed (03:05 Sat, $2.01); both merged into `pod` (`1d33df3`) and cited.** C_masked resolved the headline: V = 0.049 ≤ 0.18 → loss placement.

## Objective (unchanged)
Does on-policy GRPO leave a readable mean activation-difference trace on unrelated text the way narrow SFT does (Minder et al.)? Is any RL trace due to the data, the behaviour, or the learning rule?

## Status: DATA CLOSED. VERIFICATION COMPLETE EXCEPT GUIV'S COLUMNS. WRITING.
An independent Neel-style evaluation (`docs/EVALUATION_NEEL_STYLE.md`) rated the project high-borderline-to-accept if written up well, borderline-reject as the summary currently reads: P(exploration phase) ~45 %, ~60 % with a clean own-voice summary and a C-vs-A Figure 1. Its top objection — the headline is not dose-matched — is now the first thing the write-up addresses.

## The result, as the write-up now states it
1. **Headline (loss placement; resolved by C_masked 03:05 Sat).** **The trace is set by loss placement: prompt-token supervision → large readable trace; completion-only supervision (GRPO, masked SFT) → small trace, at matched accuracy. One C_masked seed; hedged.** Unmasked imitation SFT on A's own correct samples (C) reaches A's accuracy (0.930 vs 0.940, p = 0.77, both parsers) with a trace 16.63–22.63× A's and V 4.0–5.5×; the same corpus and recipe with completion-only loss (C_masked) reaches 0.935 (vs A 4/5, p = 1.00) with trace 0.286 = **1.36–1.85× A's**, ‖ΔW‖_F 5.84, **V 0.049** — below both A seeds and below masked D_math (0.059). **The learning-rule reading is not supported by the decisive test** (pre-stated line V ≤ 0.18 / ≥ 0.30). **The dose confound is excluded as the explanation: C_masked has C's dose (lr 1e-4 × 225) and A's trace.** C_masked·A = 0.62 (about A's own cross-seed 0.68); C_masked·C = 0.32. Limitations: one seed, one masking pattern (0.726 of tokens supervised); content-vs-position of supervision untested. Kept as background: the raw ratio factorises as ‖ΔW‖_F × V (16.63 = 4.157 × 4.001; 22.57 = 4.139 × 5.453); C's 5.0× lr×steps mismatch is a stated difference, no longer an open confound for the headline. **C is two seeds:** C s1 reproduces C s0 within 2 % on accuracy (0.925 vs 0.930, p = 1.00), trace (3.498 vs 3.488), ‖ΔW‖_F (6.958 vs 6.963) and V (0.5027 vs 0.5010), and **C s0·C s1 = 0.98 while A s0·A s1 = 0.68** — the imitation trace is a property of the data and recipe, the RL trace of the run; norm-free, so it survives every confound. With four seed pairs the raw ratio is **16.63× / 22.57× / 16.68× / 22.63×** (`results/trace_ratio_C_A_seeds.csv`) and V **4.0–5.5× (four (C, A) seed pairs: 4.00, 5.45, 4.01, 5.47)**. **Second named alternative — loss placement (`docs/T2_THEORY_PASSES.md` §1.0):** masked math SFT has V 0.059, below both A seeds; unmasking the same corpus alone takes it to 0.179 at unchanged ‖ΔW‖_F; C was trained unmasked and GRPO supervises only completion tokens. Decisive test: a completion-only C — **C_masked, run: V = 0.049 ≤ 0.18 → loss placement.** C·A = +0.505 (s0) / +0.504 (s1), above all 50 random draws; B·A = −0.13, below all 50 (orthogonal to slightly negative). "The difference is the weighting" is deleted everywhere.
2. **Assay validated (Gate 1).** Cooking SFT decodes via Patchscope to food words (7–8/20 vs null ≤ 2); norm 3.15 vs floor 0.40; position 0 excluded as a generic first-token offset. SFT arms reproduce across seeds (0.92–0.98).
3. **The RL trace is small, format-shaped, as constant as SFT's, and does not reproduce across seeds** (A s0·s1 = 0.68). Mean-offset share A 0.258 vs D_math_full 0.249 vs D 0.277 — **this is the answer to "bias term or deeper?": at this dose the RL trace is as constant as SFT's; what differs is magnitude and reproducibility. No ablation from the fine-tuned model was run, so "deeper" is untested here.** Source: `results/perposition_table_C.csv` (constancy column, neutral p1).
4. **Gate 2 survives, shrunk, with tags.** A vs D_math raw 62/6 → re-scored **22/7 (p = 0.008, suggestive only by Neel's stated standard)**. Of the 68 discordant items: D_math 42 FORMAT / 20 REASONING; A 0 FORMAT / 6 REASONING. Narrow reasoning-gain claim sayable (firewall §1). The 7th re-scored D_math-only item is a rescued one.
5. **Steering is a negative result — does not survive the parser correction (changed 2026-09-05).** The raw-parser grid clears a five-seed random null at α ≤ 0.5 (A 0.200, p = 0.013; C 0.215; D_math_full 0.285). Re-scored on the same completions (`results/steer_table_reparsed.md`): unsteered **0.790**, A α=0.25 **0.815 (p = 0.52)**, C α=0.5 **0.730**, D_math_full α=0.5 **0.650 (p = 0.0003 the wrong way)**, random 0.765–0.820. **No direction beats the unsteered base; the raw gains were the steering making the model stop.** Steering is not causal support: one paragraph under the confound section as the second demonstration of the stopping artifact, removed from the contributions list and from any "causal" language. Amplification at the α where the raw effect appears is **16–33×** (11.243 × {0.25, 0.5} / 0.17), never "~50×". Guiv's reading of the α = 1 generations: ⏳.
6. **N2 (preregistered null)** — computed day 1, unreported until Fri afternoon. As saved it is logit-lens, not Patchscope, so it cannot null the headline relevance count and cannot be regenerated. It nulls the *cosine*: **H3 fails on the primary set** (A·B = −0.13 vs null 95th pct +0.03), passes on math. Reported as a preregistered negative result.
7. **Theory scorecard (`docs/T1_THEORY_BLOCKS.md` Block 3):** **P1 half-observed** — A is format-shaped (digits, relation symbols) as predicted, but C (both seeds) is also format-shaped with no math vocabulary, so the discriminating half ("C → topic words") failed and the shape comes from the data; P2 input-gated ✓ (with gating tracking training domain, not rule: C 1.5/2.2, D_math 8.4); P3 constancy ✗; P4/H3 B-alignment ✗ (preregistered). Post-hoc refinement's C prediction ✓, labelled post hoc, dose-robust because it is a cosine. The cancellation argument got the shape right and the statistics wrong.

## Verification state
- `VERIFY.md`: 49 rows (46–49 added for C seed 1 at the C1 merge), first four columns filled, every recompute one-liner run by Chat 1 and matching the digest. **Guiv's three columns ⏳** (he runs the one-liners himself).
- Discordant block (`notes/VERIFY_discordant_block.md`) written: Guiv read all 68; 67/68 concordance with the LLM judge is **verification, not blind agreement** (he tagged after seeing its output); item 186 re-classified FORMAT→REASONING and 56 added, both logged; blinding leaks arm identity via output format (disclosed); X/Y→arm key confirmed from format signatures without opening the key.
- **Re-parser ∩ tags: 40/40** rescued items inside the 68 were tagged FORMAT, 0 REASONING; the 2 FORMAT-not-rescued (152, 184) are parser conservatism. Three independent routes agree item by item.
- Reading still owed by Guiv: Patchscope lists **blind** (5 min — still uncontaminated), steered generations (15), cooking + black-box (8).

## Integrity items (in the body as one reproducibility paragraph; full ledger in VERIFY.md/CHANGELOG)
Adapters/caches destroyed (numbers re-derivable only by retraining); arm B training curve unverifiable (pod log destroyed; eval corroborates qualitatively); three result files sync-reverted and regenerated; digest §7 p0 sentence corrected; digest was untracked until Fri 14:00; lexical baseline below null; ±2-item decode noise; blinding leak; non-blind concordance; two replication aborts + one accidental $0.09 probe pod; agent clock drift 2h15m (durations from agents are not evidence; gates are absolute times).

## Decisions in force
Both parsers or neither, everywhere. The raw ratio is "16.63–22.63×" (two decimals, four seed pairs, from `results/trace_ratio_C_A_seeds.csv`) always with C_masked's V 0.049 and 1.36–1.85× in the same paragraph; V 4.0–5.5× (four (C, A) seed pairs: 4.00, 5.45, 4.01, 5.47) is quoted as the unmasked-vs-A factor, never as a learning-rule claim. **Primary reading: loss placement. "Learning rule" only as the refuted alternative. Dose "excluded as the explanation (C_masked has C's dose)".** (Decision 2026-09-05 ~03:15: the earlier "5.4" was a rounding slip.) "C two seeds", never "C single seed". Steering is a negative result, stated as such. "Orthogonal to slightly negative", never "anti-aligned". Steering labelled raw-parser. p = 0.008 / 0.013 labelled suggestive. One story: C-vs-A; the parser confound is one paragraph under Gate 2. Fig 1 = C-vs-A, everything else faded (F1 ⏳). Claim class: narrow, hedged, one model, one task.

## Open (see OPEN_TASKS_CURRENT.md; prompts in prompts/round5/)
- **T1** theory chat → four blocks (bias-vs-deeper; dose arithmetic; scorecard; why-interesting) → Guiv rewrites in own voice. ⏳
- **F1** Chat 1 → doc fixes + Fig 1 + steering re-score. ✅ (549d7dc, 9352c9c)
- **C1** Chat 1 → merge `replication`, C two seeds, loss placement, scorecard, steering negative, C_masked insert (§5/§9/§12b, firewall, VERIFY 51–57, Fig 1). ✅
- **W4** writer → body with fixes, after F1/T1. ⏳
- **Guiv** → 30-min reading block; VERIFY columns; **executive summary (≤600 words, own voice, context→gap→claim→evidence→standard of evidence)**; form Q10–Q21; 06:00 go/extend; submit 08:30; revoke RunPod key.
- T1/T2 delivered and committed as `docs/T1_THEORY_BLOCKS.md`, `docs/T2_THEORY_PASSES.md` (PREREG v2 is T2 Pass 4).
- Post-deadline: E1 (Claude Code → dose-matched C family, seeds, trace ablation). See `docs/FUTURE_DIRECTIONS.md`.

## Risks, ranked
1. Executive summary unwritten. 2. Dose confound stated too late or too softly. 3. Body reads as LLM prose. 4. Two competing stories. 5. Replication landed and is one sentence plus a scorecard line, not a restructure. 6. The headline changed at 03:05 (learning rule → loss placement); every W4/W5 sentence that says "learning rule" as a claim is now wrong and must be rewritten, not patched.
