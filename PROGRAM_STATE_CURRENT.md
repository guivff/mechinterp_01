# PROGRAM_STATE_CURRENT.md — canonical state snapshot

**As of:** Sat 2026-09-05 ~03:45 Europe/Zurich (≈5 h to deadline 08:59; new-numbers cutoff 07:30; 07:30 go/extend decision; submit by 08:30). Numbers: `docs/RESULTS_DIGEST.md` on `pod` @ `0d9e487` or later (sole citable source) plus `results/REPLICATION_REPORT_C_masked.md` — **merged into `pod` at `1d33df3` (C2); digest carries C_masked from `198d460`/`4a3f508` onward.** All pods terminated (account shows zero). Replication branch cost ≈ $4.5; total ≈ $205.

## Objective (unchanged)
Does on-policy GRPO leave a readable mean activation-difference trace on unrelated text the way narrow SFT does (Minder et al.)? Is any RL trace due to the data, the behaviour, the learning rule — or to where the loss is placed?

## Status: DATA CLOSED. THE DECISIVE TEST RESOLVED AGAINST THE PRIOR HEADLINE. WRITING.
At 02:00 the theory pass (T2 §M0) proposed loss placement as the alternative to the learning-rule reading and a decisive test was preregistered (`PREREG.md` amendment 02:00): C_masked — arm C with prompt tokens masked, same data, same lr×steps, seed 0 — with V ≤ 0.18 → loss placement, V ≥ 0.30 → learning rule. It landed at 02:56 with **V = 0.049**. The headline interpretation changes; every prior number stands. The second independent Neel-style evaluation (`docs/EVALUATION_NEEL_STYLE_v2.md`) rates this version higher than the last: P(exploration phase) ~0.45, ~0.55 with an own-voice summary that leads with the overturn.

## The result, as the write-up now states it
1. **Headline (C / C_masked / A).** Imitation SFT on A's own correct samples (C, prompt tokens supervised) reaches A's accuracy (0.930/0.925 vs 0.940, both parsers) and leaves a neutral-text trace **16.63–22.63× larger** than A's (3.49 vs 0.21/0.155). **Masking the prompt tokens (C_masked: same data, same lr×steps, ‖ΔW‖ 5.84 vs 6.96, accuracy 0.935, p = 1.00 vs C s0) cuts the trace to 0.286 (floor 0.039) and V from 0.50 to 0.049** — 1.4–1.9× A's trace. Its direction is closer to A (cos 0.624/0.494 to A s0/s1) than to C (0.320/0.297); A's own seeds agree to 0.68, so "toward A" holds on one seed and is within A's own scatter on the other; and its Patchscope readout is A's format shape (digits, `→`, `Let`). **Claim: the size and direction of the readable trace are set by loss placement — it accounts for ~10–12× of the 16.63–22.63× gap, while the learning rule's residual has the opposite sign (RL's V is 1.9–2.6× larger).** Decomposition of the 16.63–22.63×: ~12× loss placement × 1.4–1.9× residual, where the residual is A's smaller ‖ΔW‖ (3.5×) partly offset by A's *larger* V (0.125/0.092 vs 0.049). **At matched loss placement RL and SFT traces are comparable; per unit ‖ΔW‖ RL's is larger, not smaller.** The learning-rule reading = tested alternative, not supported. The dose confound = excluded as the explanation of the C-vs-A gap (C_masked has C's dose). The D_math pair is the observation that motivated the test and anchored the 0.18 threshold (D_math_full's own V), not corroboration: 3× (0.179 → 0.059, ‖ΔW‖ −2 %) against C's 10× (‖ΔW‖ −16 %); D_math's masked-token fraction is not recorded. One C_masked seed, one masking pattern — hedged in the same paragraph. What survives as an RL-vs-SFT difference: cross-seed reproducibility (C 0.98, A 0.68).
2. **Assay validated (Gate 1).** Cooking SFT decodes to food words (7–8/20 vs null ≤ 2); norm 3.15 vs floor 0.40; position 0 excluded. SFT arms reproduce across seeds (0.92–0.98). Guiv's readings done: cooking corpus 5/5 recipes, LLM-written, two near-duplicate dishes in five draws (`notes/READ_cooking_corpus.md`); black-box: D diverges from base at token 1 on 5/5 prompts, D_math_full 1/5, A and B 0/5 under same-seed sampling, base_s1 = noise scale (`notes/READ_blackbox_rows.md`).
3. **The RL trace is small, format-shaped, as constant as SFT's, and does not reproduce across seeds.** Mean-offset share A 0.258 vs D_math_full 0.249 vs D 0.277.
4. **Gate 2 survives, shrunk, with tags.** A vs D_math raw 62/6 → 22/7 (p = 0.008, suggestive). 68 discordant: D_math 42 FORMAT / 20 REASONING; A 0 / 6. Re-parser ∩ tags 40/40.
5. **Steering** — raw-parser gains not separated from stopping; under the re-parser no direction beats base 0.79 (re-scored row citable in digest §10c; steered rescues were not audited, so it inherits an unchecked parser). Amplification needed 16–33× (steering-vector norm 0.17); T2's 13–27× uses the p1 norm 0.21 and is deliberately not harmonised.
6. **N2** logit-lens only; nulls the cosine: H3 fails on the primary set (A·B = −0.13 vs null 95th pct +0.03). Preregistered negative.
7. **Scorecard:** P1 ✓ (half), P2 ✓ with caveat, P3 ✗, P4/H3 ✗ preregistered; post-hoc C prediction ✓; **M0 loss-placement prediction ✓ (preregistered 02:00, threshold V ≤ 0.18, observed 0.049)**; the learning-rule headline ✗ on its own preregistered test.

## Verification state
- `VERIFY.md`: rows 1–50 (Chat 1, one-liners run), 51–52 reserved for Guiv's readings (notes/READ_*.md, not yet in the repo), 53–57 C_masked (Chat 1, one-liners run, outputs pasted), 58 the gap decomposition, 59–60 C_masked ‖ΔW‖ and C_masked:A ratio (Chat 1). Guiv's three columns on 1–49 ⏳.
- All human reading done: Patchscope blind ✓ (correct), 68 tags ✓ (non-blind, after the judge; 67/68 — item 186 the disagreement, item 56 added by Guiv; no blind-20 rate), steering paragraph ✓, cooking + black-box ✓. Self-report never read; omitted.

## Integrity items (one reproducibility paragraph in the body; ledger in VERIFY.md/CHANGELOG)
As before, plus: second replication pod's identity check overwrote the C s1 filename (saved separately; C s1 restored from git); adapter `adapters/C_masked_s0/final` untracked on the Mac (kept); `--arm C_masked` not accepted by the CLI so the run used `--arm C --completion-only` (D_math's exact masking path); C_masked vs C s0 row selection not verifiable (C s0 run_meta lost); stray chmod mode changes reverted before commit; the 4.02 → 4.01 rounding correction (`0d9e487`).

## Decisions in force
Both parsers or neither. V-range string: `4.0–5.5× (four (C, A) seed pairs: 4.00, 5.45, 4.01, 5.47)`. "Orthogonal to slightly negative". No per-cell "rises with EOS". **Never "GRPO is hard to detect because it never supervises prompts" — V(A) > V(C_masked).** One story: C / C_masked / A; Fig 1 = V by arm split masked/unmasked, C and C_masked adjacent, A inside the masked cluster, caption stating the preregistered threshold. Learning rule and dose kept as the two tested-and-not-supported alternatives, one sentence each. Claim class: narrow, hedged, one model, one task, one C_masked seed.

## Open (see OPEN_TASKS_CURRENT.md; prompts in prompts/round7/)
- **Chat 1** → merge `19524db`; digest/firewall/VERIFY 53–57/Fig 1/limitations; writer zip. ✅ (C2; hashes in CHANGELOG)
- **Writer** → W5 body with the W6 override. ⏳
- **Guiv** → executive summary (corrected claim sentence); VERIFY columns 1–49; form Q10–Q21; 07:30 go/extend; submit 08:30; revoke RunPod key.
- Post-deadline: content-vs-position of supervision (prompt-loss-only C; scrambled-prompt C); C_masked seed 1; ablation; PREREG v2 rewrite.

## Risks, ranked
1. Summary keeps the causal "which is why" sentence. 2. Restructuring the body at 04:00 instead of changing one paragraph and one figure. 3. Over-claiming from one C_masked seed. 4. LLM prose. 5. Missing 07:30 with the summary not in Guiv's voice → take the extension.
