# OPEN_TASKS_CURRENT.md — live queue only (Sat 2026-09-05 03:45 Zurich; T−5 h 15)

Owners: **G** Guiv · **C1** Chat 1 (Claude Code, repo, no GPU) · **R** replication chat (Claude Code, `~/repl`, branch `replication`; done, idle) · **W** writer · **WC** write-up critic · **Cowork** control plane. New numbers stop **07:30**; C_masked is the last number and it has landed. 07:30 go/extend decision (extension to Sept 11 exists; 20-hour cap unchanged). Submit by **08:30**. Prompts: `prompts/round7/`.

## Blocking the submission
1. **C1** — `C2_MERGE_C_MASKED.md`: merge `19524db`; digest §5/§9/§12b rows and the resolved claim paragraph (corrected wording — no causal "which is why"); firewall already rewritten by Cowork (apply the Cowork versions of the control-plane files from the round-7 zip, then re-check every number against the digest); VERIFY rows 53–57 with one-liners; Fig 1 = V by arm, masked/unmasked split, C and C_masked adjacent, A inside the masked cluster, caption with the preregistered threshold; limitation 6; writer zip. Report hashes, idle.
2. **W** — `W6_WRITER_OVERRIDE.md`: claim paragraph resolved to loss placement with the corrected sentence; one figure; no restructure.
3. **G** — executive summary, ≤600 words, own voice, opens with "our own headline was tested and refuted on its preregistered threshold" then the corrected claim sentence with its three hedges. Context → gap → claim → evidence → standard of evidence.
4. **G** — VERIFY.md columns on rows 1–49 (one-liners); rows 51–52 already written.
5. **G** — form Q10–Q21. Q14 leads with the two self-refutations (re-parse; C_masked). Q16 = the ledger incl. replication incidents.
6. **WC** — voice and firewall-breach pass on the summary + form (~06:00). G fixes himself.

## 07:30 decision
Go if: VERIFY closed, summary in G's voice, Fig 1 regenerated from real files, no ⏳ in the digest. Otherwise request the extension and do not rush.

## Saturday close
Recompute headline numbers once more; Toggl screenshot; doc permissions = anyone with link; paste form; upload resume; **submit by 08:30**; revoke RunPod key; keep the two adapters on the Mac.

## Done since 02:00
V-range decisions (`1ccf25b`, `0d9e487`) · C_masked run, synced, pod terminated (`19524db`, $2.01) · Guiv's cooking + black-box notes and VERIFY 51–52 · second Neel-style evaluation (`docs/EVALUATION_NEEL_STYLE_v2.md`) · control plane rewritten for the loss-placement headline.

## Dropped (final)
Everything in the previous list, plus: any C_masked seed 1 before the deadline; any harmonisation of 16–33× with 13–27×; any restructuring of the body for the new result.
