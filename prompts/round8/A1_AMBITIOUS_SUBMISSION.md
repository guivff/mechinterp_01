# A1 — "Most ambitious defensible version" chat (Cowork / Claude, extended thinking). Sat 04:30 Zurich. Output by 06:00.

You are a senior interpretability researcher helping a MATS 12.0 applicant (Neel Nanda's stream) find the strongest *true* framing of a 20-hour project. Your job is ambition inside the firewall: the most interesting, most decision-relevant, most memorable version of what the data actually show — not one number beyond it. Every sentence you write must be traceable to `CLAIM_FIREWALL.md` §1 or the digest; anything in firewall §2 is forbidden. If a stronger claim needs a number that does not exist, say what experiment would produce it, in one sentence, and stop.

Attached: `context/NEEL_FAQ.txt` (what he selects for; read the criteria, the "sanity-check your agent" section and the rejection reasons), `CLAIM_FIREWALL.md`, `PROGRAM_STATE_CURRENT.md`, `docs/RESULTS_DIGEST.md` (numbers; sole source), `docs/WRITEUP_BODY_W6.md` (the current body — dense, correct, unexciting), `docs/EVALUATION_NEEL_STYLE_v2.md`, `docs/T2_THEORY_PASSES.md`, `PREREG.md`, `results/REPLICATION_REPORT_C_masked.md`.

The finding, in one line: the size and direction of a fine-tune's readable activation-difference trace on unrelated text (Minder et al. 2510.13900) are set by which tokens carry the loss — masking prompt tokens removes ~92 % of imitation-SFT's trace at unchanged accuracy, data and dose, and lands it beside on-policy GRPO in magnitude (within 2×), direction (cos 0.62) and readout; per unit weight change the RL trace is *larger*; what stays RL-specific is cross-seed irreproducibility (0.68 vs 0.98). Preregistered threshold, observed 0.049 vs ≥ 0.30 for the alternative.

Deliver, in this order:

1. **Three candidate framings**, each ≤ 120 words, each with a working title, the one figure it would lead with, and the single sentence Neel would remember. Candidates to consider (add your own): (a) *what activation diffing measures* — a signature of loss placement, not of learning; (b) *forensics* — RL and completion-only SFT are indistinguishable under the standard diffing assay, and a large readable trace means the loss touched document-start tokens; (c) *"bias term or something deeper?"* — the big readable component can be removed without touching behaviour, so it is not what carries the capability, and the capability-bearing update is small, format-shaped and seed-specific. For each: which firewall §1 lines it rests on, which §2 line it is closest to violating, and what one experiment would make it a paper claim.
2. **Your pick and why**, in 100 words, against Neel's criteria (taste, truth-seeking, simplicity, "did I learn something").
3. **An executive summary skeleton for the pick**: section headings from the FAQ's suggested format (problem and why; takeaways; one paragraph + graph per key experiment; limitations), with one-line placeholders that name the number and the file for each slot. **Do not write the summary prose** — the applicant writes it in his own voice; the FAQ rejects LLM-written summaries. You may propose the *first sentence* only, in three variants.
4. **Five "surprising specifics"** the summary should contain (the FAQ: "specifics beat vibes: name the models, the key experiment, the surprising number"), each with its source file.
5. **What to cut from the body** to get simplicity from 3/5 to 4/5: list sections/paragraphs to demote to an appendix or delete, with a one-line reason each.
6. **The two sentences that overreach** in your own draft — find them yourself and mark them.

Rules: no adjectives about the project ("striking", "remarkable"); no "we show"; numbers only from the digest with the file beside them; one seed on the decisive arm stated wherever the claim is; never "GRPO is hard to detect because it never supervises prompts" (V(A) > V(C_masked)); never "explains Minder et al." (say "consistent with"); nothing about paper potential in the applicant's voice.
