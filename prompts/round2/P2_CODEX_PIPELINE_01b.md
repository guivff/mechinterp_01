Work on a new branch named agent01b. Follow AGENTS.md; your final message must be the report format in AGENTS.md §7. Read PROGRAM_STATE_CURRENT.md, PREREG.md, OPEN_TASKS_CURRENT.md and docs/ROUND2_LANES.md lane E (items E1, E5, E6) and docs/PLANS_BASE_VS_AMBITIOUS.md add-ons A1, A4, A6, A7. Do not change any PREREG definition; if something is ambiguous, implement the PREREG reading and note the ambiguity in VERIFY.md.

Implement, with tests on the tiny model (tests/ must stay green; add tests for every new function):

1. Block-wise estimator (E1). `readout/diff.py`: `collect_residual` returns (H, ids, positions); add `split_blocks(n_snippets, K=10, seed=0)` returning frozen disjoint index blocks; `diff_stats` accepts a block mask and also returns the mean-offset energy share (constancy as defined in docs/PROTOCOL_NOTES.md §3), block-to-block cosine, and the per-position (0–4) means for the Minder-faithful diagnostic. `readout/run_readouts.py`: emit one diff vector, one norm-matched logit-lens top-20 list, and one judge item per block, per snippet set, per layer requested (`--layers 11 15 19`), reading cached base activations from results/cache/ when present. Nulls: N1 = base split-half (block i minus block j) built from the cache; N2 = 50 random directions at η_ref; N3 = adapter from readout/make_null_adapter.py.

2. Emergence curve (A1). `readout/run_checkpoints.py`: for a run directory with checkpoints every 25 steps, run the block-wise token readout and geometry at each checkpoint at L=15 on both snippet sets; write results/curve_{arm}_s{seed}.csv (step, snippet_set, block, norm, constancy, judge item id).

3. Summaries and figures (E6, A4, A6, A7). `analysis/summarize.py`: refuse to mix files with MOCK in the name with real files; Figure 1 = block-level judge accuracy by arm × snippet set with chance, always-math, always-none, TF-IDF and N1–N3 bars (Wilson 95% over K blocks); Figure 2 = raw norm and constancy by arm and snippet set; Figure 3 = top-20 tokens for A, B, C, D, A−B, N1 (one block each chosen by seed); Figure 4 = emergence curve (norm, constancy, judge accuracy vs step with the reward curve from logs/); Table = block-mean cosine matrix incl. random and cross-seed if seeds exist; a small table for the conditional-trace test (‖d_A,math‖ vs ‖d_A,neutral‖ against the same for D); a layer-sweep panel (11/15/19) for A and D; the per-position (0–4) D diagnostic.

4. VERIFY.md merge (E5). Restore Agent 01's ledger rows from the parent of commit 8a1dcd0 and merge additively with the current file; keep both agents' rows; add the "attempt ledger" and "overnight autonomous agent work" sections if missing.

5. Run the whole path end to end on the random tiny model with a fake adapter and `judge/judge.py --dry-run`, then `summarize.py`. Everything must execute. Commit with clear messages. Do not touch grpo/ (Agent 02 / the pod runner own it) or judge/judge.py logic (Agent 03b owns it) beyond the item schema.

Report: what you ran, what changed, what you did NOT verify, and the three most likely ways the block-wise estimator is subtly wrong on the real 4B model (position handling, cache alignment between base and fine-tuned runs, dtype, hook return type).
