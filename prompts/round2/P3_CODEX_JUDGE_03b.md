Work on a new branch named agent03b. Follow AGENTS.md; your final message must be the report format in AGENTS.md §7. Read PREREG.md (judge, lexical baseline, self-report sections), OPEN_TASKS_CURRENT.md items 7–8, docs/ROUND2_LANES.md lane E3, and docs/RED_TEAM.md rows 8, 9, 17, 22. OPENROUTER_API_KEY is set as an environment secret; never print it or write it to a file.

1. Live judge calibration. Model `openai/gpt-5-mini` via OpenRouter, temperature 0, majority of 3, plus one other family (e.g. `google/gemini-2.5-flash`) for comparison. Items: the 30 hand-written synthetic items (10 cooking, 10 math, 10 nonsense token lists) plus 10 generic coherent English paragraphs (expected `none`) and 10 verse fragments (expected `poetry`). Save raw responses. Deliver results/judge_calibration.jsonl and a table: accuracy per class per model, plus the always-math and always-none baselines and the confusion matrix. If gpt-5-mini scores below 0.9 on cooking/math or labels generic English as something other than `none` more than 20% of the time, say so and propose (do not apply) a prompt fix in VERIFY.md.

2. Shuffle control. Confirm judge.py's control permutes the input↔gold pairing across items (not the visible label order). Add the always-math / always-none baselines and the confusion matrix to judge/lexical_baseline.py's summary output. Report per-block accuracy when items carry a `block` field (K=10), with Wilson 95% intervals over blocks, and a note if any cell has fewer than 10 unique inputs.

3. External TF-IDF reference corpus. Build data/lexical_reference/{math,cooking,law,medicine,poetry,none}.jsonl with 50 documents each (100–300 tokens), generated via OpenRouter or drawn from public datasets; `none` = generic English (news, fiction, forum) with no dominant domain. Deduplicate; record a manifest with hashes and provenance. Retrain the TF-IDF (1–2 gram) + logistic-regression baseline on this corpus only; test on readout texts; assert that no readout text (or its 8-gram shingles) appears in the training corpus. Also provide a variant that classifies a *token list* (the logit-lens top-20) by treating it as a bag of tokens.

4. Self-report scoring. Add `judge/selfreport.py` that sends each self-report line to the same judge with the same labels, and outputs per-arm label histograms. Include a `base` and `N3` row in the schema.

5. Tests for all of the above on the dry-run path; keep tests/ green. Commit with clear messages. Do not touch readout/ or grpo/.

Report: calibration table, corpus stats and 3 random documents per class, leakage-check result, what you did NOT check, and the three most likely ways the judge could manufacture a false positive on the real data (class imbalance, `none` under-prediction on fragmentary token lists, surface tokens).
